<%= form_for(@consider_cart) do |f| %>
  <% if @consider_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consider_cart.errors.count, "error") %> prohibited this consider_cart from being saved:</h2>

      <ul>
      <% @consider_cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
