<%= form_for(@consider_pet) do |f| %>
  <% if @consider_pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consider_pet.errors.count, "error") %> prohibited this consider_pet from being saved:</h2>

      <ul>
      <% @consider_pet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :consider_cart_id %><br>
    <%= f.text_field :consider_cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
