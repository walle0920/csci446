<%= form_for(@line_pet) do |f| %>
  <% if @line_pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_pet.errors.count, "error") %> prohibited this line_pet from being saved:</h2>

      <ul>
      <% @line_pet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :fostered_id %><br>
    <%= f.text_field :fostered_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
